spring:
  application:
    name: AuthenticationAndAuthorizationFilter
  datasource:
    url: jdbc:postgresql://localhost:5432/userandroles
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update   # (create / create-drop / validate / none)
    show-sql: true
    properties:
      hibernate:
        #        jdbc:
        #          time_zone: Asia/Kolkata
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect



my:
  secret:
    key: 123456789

jwt:
  secret:
    key: aapka-bahut-lamba-aur-secure-secret-key-yahan-hona-chahiye