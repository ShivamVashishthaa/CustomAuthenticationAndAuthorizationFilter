spring:
  application:
    name: JwtAuthentication
  datasource:
    url: jdbc:postgresql://localhost:5431/userandroles
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update   # (create / create-drop / validate / none)
    show-sql: true
    properties:
      hibernate:
        #        jdbc:
        #          time_zone: Asia/Kolkata
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

my:
  secret:
    key: pyCaBIrMfW1ansDDskYGZv0xOIldPS2FgOJ5RedD97w=